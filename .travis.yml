language: minimal

stages:
  - test
  
jobs:
  include:
    - stage: test
      language: matlab
      install: 
        - git clone --depth 1 https://github.com/MHKiT-Software/MHKiT-Python MHKIT
        - cd MHKIT
        - cd mhkit-python
        - pip install -e .
      script: 
        - matlab -batch "import mhkit.*; matlab.addons.install('mhkit.mltbx'); runMatlabTests(); results = runTests('reportHTML', false, 'reportPDF', false), assertSuccess(results);"
      if: branch = master
